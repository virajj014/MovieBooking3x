export interface MovieCardType {
    title: string;
    imageUrl: string;
    _id: string;
    rating: number;
    type: string;
}

export interface CelebrityCardType{
    name: string;
    imageUrl: string;
    _id: string;
    role: string;
}